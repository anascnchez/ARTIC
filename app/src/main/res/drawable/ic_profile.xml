<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Estado cuando el componente está seleccionado -->
    <item android:drawable="@drawable/icon_profile" android:state_selected="true" android:tint="?attr/colorPrimary" />
    
    <!-- Estado por defecto (no seleccionado) -->
    <item android:drawable="@drawable/icon_profile" android:tint="?attr/colorControlNormal" />
</selector>